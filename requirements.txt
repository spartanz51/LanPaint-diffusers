# Runtime dependencies for lanpaint_release
# Tested with Python 3.12 and CUDA-enabled PyTorch in this workspace.
#
# LanPaint is not on PyPI; install from GitHub (provides "from LanPaint.lanpaint import LanPaint"):
#   https://github.com/scraed/LanPaint
git+https://github.com/scraed/LanPaint.git
torch==2.8.0
torchvision==0.25.0
# diffusers 0.37.0.dev0 is not on PyPI; install from GitHub (needed for Flux2Klein, Z-Image, etc.)
git+https://github.com/huggingface/diffusers.git
transformers==4.57.6
accelerate==1.12.0
safetensors==0.7.0
numpy==2.3.2
Pillow==11.3.0
tqdm==4.67.1
requests==2.32.5
huggingface-hub==0.36.0
scipy==1.17.0
sentencepiece==0.2.1
protobuf==6.33.4
